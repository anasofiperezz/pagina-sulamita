{
  "name": "pagina-sulamita",
  "version": "1.0.0",
  "private": true,
  "description": "Sitio web y API de Papelería Sulamita.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "setup-db": "node setup-db.js",
    "migrate-json": "node migrate-json-to-db.js",
    "check": "node --check server.js && node --check db.js && node --check setup-db.js && node --check migrate-json-to-db.js"
  },
  "engines": {
    "node": ">=20"
  },
  "keywords": [
    "papeleria",
    "tienda",
    "express",
    "postgresql"
  ],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "cloudinary": "^2.10.0",
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "multer": "^2.1.1",
    "pg": "^8.20.0"
  }
}